# OXPID

This repository is based on the **OpenDet** codebase.  
To use our method, simply replace the original `fast_rcnn.py` file with the provided one.  
In addition, the implementation calls the **PNPL** and **D2D** modules.
